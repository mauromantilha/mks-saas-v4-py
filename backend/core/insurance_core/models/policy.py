"""Policy models are introduced incrementally.

The current codebase still uses `operational.Apolice` and `operational.Endosso`.
When we migrate fully to `insurance_core`, the canonical policy aggregates will live here.
"""

